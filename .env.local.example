# IsoCity Agent Bridge Configuration
# Copy this file to .env.local and set your own token

# Token for authenticating agent API requests
# Use any random string (e.g., generate with: openssl rand -hex 32)
AGENT_BRIDGE_TOKEN=your-secret-token-here
NEXT_PUBLIC_AGENT_BRIDGE_TOKEN=your-secret-token-here
